"use strict";

window.dodoTechnicalDocuments = Object.freeze({
  tokens: [
    "# tokens.ts｜设计 Token 真源",
    "",
    "> **案例展示名**：`tokens.ts` 代表 Token 层，不对应单一文件。实际实现由 `palette.ts` 与 `token.ts` 共同承载。",
    "",
    "## 解决什么问题",
    "",
    "把颜色、间距、圆角和阴影从页面样式中抽离，形成可复用的基础变量；组件只消费语义 Token，不直接引用原始色值。这样既能保证页面一致，也能为主题切换和后续自动生成提供稳定输入。",
    "",
    "## 文件对应",
    "",
    "- `palette.ts`：维护灰阶、品牌色、状态色和导航色等原子色板。",
    "- `token.ts`：将色板注入 Ant Design，并导出组件可消费的设计 Token。",
    "- `CNAP-代码化组件与Design-Tokens.md` 第 2 节：记录 Token 的设计意图与使用边界。",
    "",
    "## 核心规则",
    "",
    "1. 原子色板只描述颜色本身，不绑定业务含义。",
    "2. 页面与组件优先使用语义 Token，避免散落硬编码。",
    "3. 间距、圆角和阴影采用统一刻度，减少相近但不一致的数值。",
    "4. 品牌色只用于需要强调的状态，不扩散到所有交互控件。",
    "",
    "```ts",
    "export const palette = {",
    "  gray, primary, brand, navigation, success, warning, error,",
    "} as const;",
    "",
    "export const themeConfig = {",
    "  token: {",
    "    colorPrimary: colors.primary,",
    "    colorInfo: colors.info,",
    "    colorLink: colors.link,",
    "  },",
    "};",
    "```"
  ].join("\n"),
  theme: [
    "# theme.ts｜主题与组件配置",
    "",
    "> **案例展示名**：`theme.ts` 代表主题配置层。实际代码集中在 `themes/presets.ts`，并结合组件级配置共同生效。",
    "",
    "## 解决什么问题",
    "",
    "把设计判断转成组件库能够直接执行的主题规则。它负责将语义 Token 映射到 Button、Input、Select、Menu 等具体组件，并限制品牌色的使用范围，避免全局主题变量污染中性交互界面。",
    "",
    "## 文件对应",
    "",
    "- `themes/presets.ts`：定义主题预设、全局 Token 和组件级覆盖。",
    "- `token.ts`：提供 Ant Design 的基础主题配置与导出 Token。",
    "- `CNAP-代码化组件与Design-Tokens.md` 第 3—4 节：记录组件配置与注入方式。",
    "",
    "## 核心规则",
    "",
    "1. 全局 Token 只建立统一基线，差异规则下沉到组件级配置。",
    "2. Input、Select、Menu 等中性交互控件保持灰黑体系。",
    "3. Switch、Progress 等强调组件才使用品牌色。",
    "4. 所有主题由统一入口注入，页面不自行覆盖组件状态。",
    "",
    "```ts",
    "export const themePresets = {",
    "  cnap2: {",
    "    token: {",
    "      colorPrimary: palette.brand[6],",
    "      colorText: semantic.text.primary,",
    "      colorBgLayout: semantic.bg.page,",
    "    },",
    "    components: {",
    "      Button: { borderRadius: radius.xl4 },",
    "      Input: { borderRadius: radius.lg },",
    "      Select: { optionSelectedBg: semantic.state.component.selectActive },",
    "    },",
    "  },",
    "};",
    "```"
  ].join("\n")
});
